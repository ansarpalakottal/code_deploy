---
# vars file for common

war_file: sample.war
war_file_deploy: sample
war_file_down_loc: 'https://github.com/ansarpalakottal/firstproject/blob/master/sample.tar.gz'
war_file_loc: '/local/apps/AIS'
date: "{{ lookup('pipe', 'date +%Y%m%d-%H:%M:%S') }}"
dst: '/tmp/sample.tar.gz'